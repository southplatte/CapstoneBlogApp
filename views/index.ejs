<%- include("header.ejs"); %>

<h1>Welcome To My Blog - An open list of articles I have written.</h1>
<ul>
    <% if(posts.length > 0) { for(var i = 0; i < posts.length; i++) { %>
        <li> <a href="viewpost/<%= i %>"><%= posts[i].postTitle %></a></li>
    <% } } else { %> <li>No posts to show</li><% } %>


</ul>
<%- include("footer.ejs"); %>